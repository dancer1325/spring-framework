* how does get the response?
  * request -- is executed through -- `ClientHttpRequestFactory`
    * `ClientHttpRequestFactory` -- is normally got from -- `RestTemplate`
* uses
  * scenarios / multiple remote services & some need to be called (!= mocked)