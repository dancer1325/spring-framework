* uses
  * customizing `ConfigurableMockMvcBuilder`
    * _Example:_ via TP library / wants to provide shortcuts -- for setting up a -- MockMvc
* if you want to plug an implementation of this interface -> use `ConfigurableMockMvcBuilder.apply()`