* == subclass of `ProtobufHttpMessageConverter` / 
  * enforces using
    * Protobuf 3+
    * "com.google.protobuf:protobuf-java-util" 3+
  * allows -- via -- `com.google.protobuf:protobuf-java-utilJsonFormat`
    * custom JSON parser
    * custom printer configurations